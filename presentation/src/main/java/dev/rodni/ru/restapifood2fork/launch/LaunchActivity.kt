package dev.rodni.ru.restapifood2fork.launch

import android.os.Bundle
import dagger.android.support.DaggerAppCompatActivity

class LaunchActivity : DaggerAppCompatActivity() {

    //todo inject by dagger view model factory and navigator

    //todo attach to a view model

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        //todo make onSuccess case navigate to home activity
    }
}
